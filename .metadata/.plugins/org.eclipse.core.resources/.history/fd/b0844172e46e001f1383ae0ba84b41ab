package com.lucas.demo.service;

import javax.xml.bind.JAXBException;

import com.lucas.demo.model.Pedido;

public class Main {
    public static void main(String[] args) {
        String xml = "<pedido><id>123</id><quantity>2</quantidade><descricao>Produto de teste</descricao></pedido>";
        
        PedidoServico pedidoService = new PedidoServico();
        
        try {
            Pedido pedido = pedidoService.xmlParaPedido(xml);
            
            // Exibir os dados do objeto Pedido
            System.out.println("ID: " + pedido.getId());
            System.out.println("Quantidade: " + pedido.getQuantidade());
            System.out.println("Descrição: " + pedido.getDescricao());
            
        } catch (JAXBException e) {
            e.printStackTrace();
        }
    }
}